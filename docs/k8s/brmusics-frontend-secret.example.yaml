# =============================================================================
# Secret — brmusics-frontend (produção)
# Autenticação própria: o frontend não armazena segredo JWT (fica só na API).
# Este arquivo permanece como reserva para futuros segredos de runtime.
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: brmusics-frontend-secret
  namespace: brmusics
type: Opaque
stringData: {}
