services:
  projetoA-frontend:
    build: .
    container_name: projetoA-frontend
    network_mode: net_sps
    ports:
      - "3002:3002"
    restart: always
    image: hub.sps.ce.gov.br/conecta/projetoa-frontend
    env_file:
      - ./config/projetoA-frontend.prod.env
